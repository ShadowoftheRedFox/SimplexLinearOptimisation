package optim.optim.src.simplex.data;

/** Type of goal we want to achieve with the objective function. */
public enum GoalType implements SimplexData {
    /** Maximization. */
    MAXIMIZE,
    /** Minimization. */
    MINIMIZE
}
